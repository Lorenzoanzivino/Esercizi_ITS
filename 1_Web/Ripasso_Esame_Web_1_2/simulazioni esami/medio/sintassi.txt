---------------------------------------------------------------------------------------------------------------------------------------------
1. <div style={ bg-danger }></div> ❌
---------------------------------------------------------------------------------------------------------------------------------------------
2. const somma = (a, b) => a+b;
---------------------------------------------------------------------------------------------------------------------------------------------
3. const {nome, eta} = Utente; ❌
---------------------------------------------------------------------------------------------------------------------------------------------
4. A) onClick={deleteUser(5)} ❌ onClick={() => deleteUser(5)}
---------------------------------------------------------------------------------------------------------------------------------------------
5. const arr2 = [ ...arr1 , 3 ];
---------------------------------------------------------------------------------------------------------------------------------------------
6. import Header from './Header';
---------------------------------------------------------------------------------------------------------------------------------------------
7. <div className="box">
---------------------------------------------------------------------------------------------------------------------------------------------
8. setUtente(prev => {return{...prev,}} ) NON HO CAPITO COSA MI HAI CHIESTO ❌ setUtente(prev => prev + 1)
---------------------------------------------------------------------------------------------------------------------------------------------
9. const classeCSS = bg-text-danger ; ❌ const classeCSS = `btn-${colore}`;
---------------------------------------------------------------------------------------------------------------------------------------------
10. <> </> oppure < />
---------------------------------------------------------------------------------------------------------------------------------------------
11. const obj = { nome : "Luca" }; ❌ const obj = { nome };
---------------------------------------------------------------------------------------------------------------------------------------------
12. 
items.map(item => (
    <div key={item.id} >
        <h1>{item.title}</h1>
    </div>
))

oppure 

items.map((item, index) => (
  <div key={index}>
    <h1>{item.title}</h1>
  </div>
))
---------------------------------------------------------------------------------------------------------------------------------------------

6/12