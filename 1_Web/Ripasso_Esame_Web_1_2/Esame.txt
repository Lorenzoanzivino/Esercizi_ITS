nell'html se abbiamo bisogno posso di mandare a capo dei tag inline posso usare 
display align block o una cosa del genere

i flex -> display:flex hanno direzione sull'asse x e y che possono essere definite
dalle proprietà row e column 

rivedere i selettori, le pseudoclassi

javascript -> assegnazione per valore (i primitivi-> numeri, bool, stringhe) o riferimento (gli oggetti-> array, classi, oggetti)
singlethread -> unico thread di esecuzione
sincrono

let (scope istruzione ovvero vive all'interno delle {}),const e var (scope funzionale ovvero vive all'interno delle funzioni + è HOIST ovvero la porta in cima allo script)

array 

oggetti

destrutturazione

spread operator

map

filter

operatore ternario

import export

promise

differenza tra arrow function e function classica -> le function classica hanno gli arguments ovvero array associativi [parametro,valore,parametro,valore]
le function classiche hanno this che fanno riferimento a se stesse mentre le arrow function hanno il this relativo al contesto dove vengono chiamate

DOM -> rappresentazione dell'alberatura della pagina html con tutti gli elementi costituenti, funzioni ecc ecc 
+ eventi -> addeventlistener ecc sempre collegate al DOM

async call -> 
let a = ''
fetch(url).then(pippo => a = ris)
log(a)

react:

usestate
useeffect
useref
usecontext
props
functional return 
route
render condizionale
short circuits
operatore ternario

react -> riusabilità , blocchi di codici riutilizzabili

un componente è fatto da una funzione (fatto da componente funzionali) e un componente ha delle caratteristiche per funzionare:
deve restituire un jsx 
deve avere la lettera maiuscola e deve essere esportato con la lettera maiuscola

come è fatto lo scaffolding di react?  
dentro index.html ed è il punto di ingresso della pagina che è una singlepage ovvero che non ricarica mai la pagina ci sta l'unico create element by id di tutta l'app react

domanda d'esempio-> perchè quando facciamo i map all'interno di un jsx stampa i valori? il mpa ci restituisce gli elementi dell'array che gli ho mandato restituendomeli in base alla logica che gli ho dato io 


produrre un layout simile a quello mostrato con i css

elementi html 
quando creiamo una pagina la pagina viene creata con un flusso frequenziale 
tutti gli elementi hanno una proprietà intrinseca che è il position ed è position:static
rimangono fissi dove sono
quando si vogliono dei layout più articolati possiamo fare una cosa tipo creare dei div dentro un altro contenitore e quei div sono statici 
si toglie position static e si mette position: relative -> cosa fa: permette di posizionare l'elemento nello spazio in relazione alla sua posizione originale (si sposta lasciando dello spazio vuoto che è occupato qualcosa), se B si sovrappone ad A chi si vede? lo decide lo z index 
attivando 5 attributi:
top
left
right
bottom
z index 

per spostare B faro
.b{
top: -10px
left: 20px
right: 

abbiamo anche position:absolute che dipende dal primo elemento statico padre 
abbiamo anche position:fixed che si comporta uguale ad absolute tranne che con abs se si scorre la pagina il blocco che si è spostato scompare con fixed rimane fisso

abbiamo position:sticky che si comporta come relative solo che rimane appiccato quando scorro


i flexbox -> perchè usare justify content e align 

z index -> ha un valore numerico di base 1

'cosa fa il position:relative?' permette il posizionamento in relazione a se stesso



NON POSSIAMO USARE UN RISULTATO DI UNA CHIAMATA ASINCRONA DENTRO UN FLUSSO SINCRONO

promiseall


differenza tra async await e then 

filtra un array di 10 numeri 

differenza tra === e == 


props